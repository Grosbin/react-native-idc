import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

export const DetailPrayers = () => {
  return (
    <View>
      <Text>DetailPlayer</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
