import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

export const NoticesScreen = () => {
  return (
    <View>
      <Text>Anuncios Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
